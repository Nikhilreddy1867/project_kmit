# Phase 1 - AI Governance Platform : income-classification baseline
# Lower bounds only, so pip resolves wheels that exist for your Python version.
# Tested on Windows 11 + Python 3.14 (64-bit).

ucimlrepo>=0.0.7        # UCI dataset fetcher (Adult / Census Income = id 2)
pandas>=2.2.0           # dataframes, CSV I/O
numpy>=2.1.0            # numerics
scikit-learn>=1.5.0     # Pipeline, ColumnTransformer, LogReg, RandomForest, metrics
xgboost>=2.1.0          # XGBClassifier (gradient boosting)
matplotlib>=3.9.0       # confusion-matrix / comparison PNGs
joblib>=1.4.0           # model serialisation (.joblib)

# --- Phase 5: read-only governance API (app/) -------------------------------
fastapi>=0.115.0        # API framework + automatic Swagger/OpenAPI docs
uvicorn[standard]>=0.30.0  # ASGI server
pytest>=8.0.0           # test runner for tests/
httpx>=0.27.0           # required by fastapi.testclient; also the dashboard's HTTP client

# --- Phase 6: local governance dashboard (dashboard/) -----------------------
streamlit>=1.40.0       # dashboard UI
plotly>=5.24.0          # interactive charts

# --- MAAT model intake: uploads + policy gates (app/onboarding, app/gates) ---
# FastAPI needs this to parse multipart/form-data. Without it the model-intake
# endpoints fail at import time, not at request time, so it is a hard requirement
# rather than an optional extra.
python-multipart>=0.0.9
